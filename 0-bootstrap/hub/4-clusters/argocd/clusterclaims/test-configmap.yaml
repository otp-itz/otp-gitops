apiVersion: v1
kind: ConfigMap
metadata:
  name: complex-injection-test
  annotations:
    "redhat-cop.redhat.io/patch": |
      metadata:
        annotations:
          hello: "{{ (lookup "hive.openshift.io/v1" "ClusterClaim" "rhacm-clusterpools" "small-cute-cluster").spec.namespace }}"
  namespace: "rhacm-clusterpools"
data:
  pippo: pluto 